/**
 * LOOP Component definitions and metadata
 * Unified export point for all LOOP component-related data
 */

// Re-export domain models
export { LOOPComponent, type LOOPComponentInfo } from "../models/generate-models";

// Re-export constants
export { LOOP_COMPONENTS } from "./loop-constants";
