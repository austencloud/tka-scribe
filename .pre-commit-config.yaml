# Minimal pre-commit - Ruff only (no blocking hooks)
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.7
    hooks:
      - id: ruff-check
        args: [--fix, --exit-zero]
      - id: ruff-format