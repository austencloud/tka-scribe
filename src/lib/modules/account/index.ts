// Account Module Exports
// Note: Tab components are intentionally not exported to avoid naming conflicts
// with other modules (e.g., LibraryTab). Import them directly if needed.
export { default as AccountModule } from "./AccountModule.svelte";
