/**
 * ShareMode.ts
 *
 * Defines the top-level mode for Share Hub.
 * User chooses between exporting a single media piece or a composite of two pieces.
 *
 * Domain: Share Hub - Core Mode Selection
 */

export type ShareMode = 'single' | 'composite';
