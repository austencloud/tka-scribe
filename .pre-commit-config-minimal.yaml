# Minimal pre-commit - should never block commits
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.7
    hooks:
      - id: ruff-format
        args: [--check]  # Only check, don't modify files
        types: [python]
        fail_fast: false
        verbose: false
